<!DOCTYPE html>
<div class ="col-6">
 <il>
 <h4>商品一覧(全<%= @items_count %>件)</h4>
 </il>


 <div class="card-group">
  <% @items.each do |item| %>

  <div class="card">
   <il>
    <%= link_to "/items/#{item.id}" do %>
    <%= attachment_image_tag item, :image, :fill, 80, 60 %>
    <% end %>
   </il>
   <div class="card-body">
     <p class="card-title"><%= item.name %></p>

   <p class="card-text">
   ¥<%= item.price. to_s(:delimited) %>
   </p>
    </div>
     </div>



  <% end %>

  </div>
  <div class = "items">
  <%= link_to '全ての商品を見る', items_path %>
　</div>
</div>
