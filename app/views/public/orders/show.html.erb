<!DOCTYPE html>
<div class="col-md-8 offset-md-1">

 <h4>注文履歴詳細</h4>

 <table class="table table-hover table-inverse">
     <thead>
      <tr>
      </tr>
     </thead>
    <tbody>
      <p>注文情報</p>
     <tr>
       <td>
         <td>注文日</td><td><%= @order.created_at.strftime('%Y/%m/%d') %></td>
      </td>
     </tr>
     <tr>
      <td>
         <td>配送先</td><td>〒<%= @order.postal_code %> <%= @order.address %> <%= @order.name %></td>
      </td>
     </tr>
     <tr>
      <td>
          <td>支払方法</td><td> </td>
      </td>
     </tr>
  </div>
 
  </tbody>
</table>



</div>
</main>